@model IEnumerable<AccountantSystemv5.Models.FulfillmentSSCD>

@{
    ViewData["Title"] = "Index";
}

<h2>FulfillmentSSCD Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DividendDeclarationDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DividendPerShare)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.StockSubscription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DividendDeclarationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DividendPerShare)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockSubscription.StockID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DividendID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DividendID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DividendID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
