@model IEnumerable<AccountantSystemv5.Models.InflowPurchaseInventory>

@{
    ViewData["Title"] = "Index";
}

<h2>InflowPurchaseInventory Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.QuantityReceived)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.InventoryReceiptPrice)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Inventory)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuantityReceived)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InventoryReceiptPrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Inventory.InventoryListPrice)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InventoryReceiptID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InventoryReceiptID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InventoryReceiptID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
